name = "copmapny-timeline"
compatibility_date = "2026-06-16"

# Static-only site: serve the generated policy library (docs/) as Worker assets.
# The deploy command `wrangler versions upload` reads this [assets] directory.
[assets]
directory = "./docs"
